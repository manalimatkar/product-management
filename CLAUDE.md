# CLAUDE.md

Context for Claude Code working in this repository. Read this before making changes.

## What this repository is

`product-management` is a **product-management and governance framework**, not application code. It defines the process by which product intent (design source material) becomes governed, traceable business scope, and then governed technical work -- via a Business Agent, a Business Owner approval gate, a Technical Agent, and an Architect approval gate -- before anything reaches an engineering repository. See [README.md](README.md) and [PRD.md](PRD.md) for the full picture; [PRD.md](PRD.md) section 5 has the end-to-end workflow diagram.

The repository is intentionally domain-agnostic (PRD.md, FRAMEWORK-CONFIGURATION-SPEC.md) -- it's built to apply beyond UI/design-led products, not just to software features with screens.

## Core pipeline (memorize this shape)

```
Source Material -> Design Handoff Bundle (optional) -> Design Analysis
  -> Business Requirements (Epic + Stories + Acceptance Criteria)
  -> Business PR -> Business Owner Approval -> Merge
  -> Technical Agent -> Canonical Task / Spike -> Architect Approval
  -> Technical Ready -> Developer Agent -> Implementation PR (engineering repo)
```

Two hard human gates: **Business Owner approval** (authorizes technical analysis) and **Architect approval** (authorizes development). Both are meant to be structurally enforced, not left to agent discipline.

## Document inventory

**Reorganized 2026-09-01**: 24 of the 29 root-level docs moved into `specs/` and `templates/` (the fillable formats, 6 files) -- root now holds only README.md, CLAUDE.md, PRD.md, IMPLEMENTATION-PLAN.md, and SOURCE-REGISTRY.md. `specs/` grew to 19 files the same day with the addition of TECHNICAL-AGENT-WORKFLOW.md (open item 4, below), then to 20 files 2026-09-03 with the addition of EXECUTION-ADAPTER-SPEC.md (open item 14, below). Every internal markdown link repo-wide was rewritten and verified (202 links checked, all resolve). Filenames themselves did not change, only their folder -- `specs/DESIGN-HANDOFF-BUNDLE-SPEC.md`, not a rename. This is separate from, and does not change, ARTIFACT-STORAGE-SPEC.md's convention for generated *instances* (`sources/`, `design/`, `analysis/`, `requirements/`, `business-prs/`, `tasks/`) -- those already had their own organization and were left untouched.

Each artifact type in the pipeline has a `-SPEC.md` (the rules) and usually a `-TEMPLATE.md` (the fillable format), both now under their respective folders:

| Artifact | Spec | Template |
| --- | --- | --- |
| Design Handoff Bundle | specs/DESIGN-HANDOFF-BUNDLE-SPEC.md | templates/DESIGN-HANDOFF-BUNDLE-TEMPLATE.md |
| Design Analysis | specs/DESIGN-ANALYSIS-SPEC.md, specs/DESIGN-ANALYSIS-REVIEW-SPEC.md | templates/DESIGN-ANALYSIS-TEMPLATE.md |
| Business Requirements | specs/BUSINESS-REQUIREMENTS-SPEC.md | templates/BUSINESS-REQUIREMENTS-TEMPLATE.md |
| Business PR | specs/BUSINESS-PR-SPEC.md | templates/BUSINESS-PR-TEMPLATE.md |
| Canonical Task / Spike | specs/CANONICAL-TASK-SPEC.md | templates/CANONICAL-TASK-TEMPLATE.md |

Cross-cutting documents (no single artifact owns these; they govern the whole pipeline), all in `specs/`:

- **EVIDENCE-SPEC.md** -- the evidence rule: every generated requirement must trace back to a source. Defines the canonical classification vocabulary (Explicit / Strongly Implied / Assumption / Technical Unknown / Decision Required / Human Provided) and the full traceability chain from a design element to a canonical Task.
- **BUSINESS-AGENT-WORKFLOW.md** -- the ordered, end-to-end Business Agent procedure (input contract, 15 processing steps, output contract, and what the Business Agent must never do), synthesized from the specs above.
- **TECHNICAL-AGENT-WORKFLOW.md** -- the mirrored contract for the Technical Agent side (input contract, 9 processing steps, output contract, and what it must never do), synthesized from TECHNICAL-HANDOFF.md and CANONICAL-TASK-SPEC.md. Also defines the optional Technical Plan (section 4.1) and the engineering-PR `Traces to:` reference format (section 6.1).
- **ARTIFACT-RELATIONSHIP-MODEL.md** -- the structural data model: canonical ID format per artifact, cardinality between every pair (e.g. Story:Task is 1:N, Requirement:Story is N:N), and versioning propagation rules. This is the source of truth for cardinality -- if any other doc's prose implies a different cardinality, this document wins.
- **ARTIFACT-STORAGE-SPEC.md** -- the physical layout: where every artifact type's file lives and how it's named (`<root>/<platform-slug>/[<app-slug>/]<feature-slug>/<type>-<feature-slug>-<id>.md`), so filenames alone identify what a file is and the chain from Design Handoff Bundle to Task can be followed by filename. This is the source of truth for storage -- distinct from ARTIFACT-RELATIONSHIP-MODEL.md, which is structure/cardinality, not location.
- **GITHUB-PLATFORM-ADAPTER-SPEC.md** -- the chosen "GitHub-light" platform adapter: exactly how every artifact type maps onto GitHub Issues, PRs, and labels once a real repository exists, filling FRAMEWORK-CONFIGURATION-SPEC.md section 13. Files stay canonical (per ARTIFACT-STORAGE-SPEC.md); GitHub objects are a tracking/review layer on top, not a second source of truth.
- **EXECUTION-ADAPTER-SPEC.md** -- the sibling adapter for *execution* rather than storage: which AI platform (Claude, ChatGPT, or Copilot) actually runs a pipeline stage, what it's given (the relevant workflow document plus that stage's Input Contract) and must return (that stage's Output Contract), and how any single stage can be invoked in isolation on any of them. New 2026-09-03.
- **HANDOFF-SEQUENCE.md** -- the same pipeline as a PlantUML sequence diagram (who does what, in order), with a note at every governing rule citing its spec section.
- **AGENT-RESPONSIBILITIES.md**, **TECHNICAL-HANDOFF.md**, **BUSINESS-REPOSITORY-WORKFLOW.md**, **REQUIREMENTS-VERSIONING-SPEC.md**, **PRODUCT-SOURCE-MATERIAL-SPEC.md**, **FRAMEWORK-CONFIGURATION-SPEC.md** -- foundational specs, present before this session's additions. **IMPLEMENTATION-PLAN.md** stays at root (roadmap doc, not a governing spec).

Every document's own "Relationship to Other Artifacts" section states what it depends on and what depends on it -- check that section before editing one, to see what else might need a matching update.

## The pattern this repo has been built with

When asked to add or check something in this repo:

1. **Grep the whole repo first.** Things are frequently already partially covered, scattered across two or three documents. Report precisely what exists (with section citations) and what's actually missing before writing anything new.
2. **Build only the missing piece**, as a document that cites existing specs rather than duplicating their content. If a new doc restates a rule from another doc, link to the source section instead of copying it, and note which one is authoritative if they ever conflict.
3. **Resolve genuinely undefined edge cases explicitly**, with stated rationale, rather than leaving a new placeholder -- but flag each one as a default that can be overridden, not settled law.
4. **Cross-link everything.** A new document gets added to README.md's Documents list, and every existing document it relates to gets a short one-line pointer back to it (light-touch edits, not rewrites).
5. **Naming convention**: `ALL-CAPS-WITH-DASHES.md`, ending in `-SPEC` (rules), `-TEMPLATE` (fillable format), `-MODEL` or a descriptive name for cross-cutting docs.

## Format preferences (established, don't relitigate)

- **No HTML** for artifacts or diagrams in this repo's context.
- **Diagrams**: PlantUML for sequence/interaction diagrams (valued specifically for note support); Mermaid where it fits better. Pick per diagram type -- don't default to one for everything.
- **Always ask before introducing a new format or tool** not already established in the working conversation.
- Markdown is the default format for every document in this repository.
- **2026-08-31: the Design Handoff Bundle no longer uses JSON.** `bundle.json` and `screens/*.design.json` were retired -- the Design Handoff Bundle is now `bundle.md` (YAML frontmatter + Markdown manifest) and `screens/*.md` (YAML frontmatter + Markdown tables). This also closed a real gap: PRD.md always named several design-source shapes (structured bundle, bare Figma link, written md + screenshots, Claude Design wireframes) but only the structured bundle had a template. DESIGN-HANDOFF-BUNDLE-SPEC.md section 6.1 now shows all four scaling through the same format. See DESIGN-HANDOFF-BUNDLE-SPEC.md and DESIGN-HANDOFF-BUNDLE-TEMPLATE.md for the new structure; the cart-optimization dry run was converted to match.

## Known open items (not yet done)

1. **Artifact storage mapping -- RESOLVED, both halves.** (a) Naming and folder layout: see ARTIFACT-STORAGE-SPEC.md -- every artifact type nests under `<root>/<platform-slug>/[<app-slug>/]<feature-slug>/` with a self-describing `<type>-<feature-slug>-<id>.md` filename. Terminology revised 2026-09-01: `product-slug` renamed to `platform-slug`, and an optional `app-slug` level added for platforms with multiple distinct applications -- see item 12 below. (b) Storage *medium*: decided 2026-08-31 as "GitHub-light" (chosen from a two-option comparison against a richer "GitHub-native" alternative) -- see GITHUB-PLATFORM-ADAPTER-SPEC.md and the now-filled FRAMEWORK-CONFIGURATION-SPEC.md section 13. Files stay canonical; Epic/Story/Task/Spike become GitHub Issues; Business PR becomes a real GitHub PR whose diff is the changed files; workflow state is two separate label groups (`status:` and `readiness:`/`tech-ready:`), deliberately never merged into one, mirroring the status-vs-readiness split used throughout this repository.
   - Decided 2026-08-31: no git/GitHub setup yet. First dry-run example ran as plain files; a real GitHub repo is a deliberate follow-up step, not a prerequisite for the dry run -- this adapter is designed, not yet exercised against a real repository.
   - Decided 2026-08-31: the cart-optimization dry-run files were migrated to the new naming convention immediately rather than left on the old one -- see ARTIFACT-STORAGE-SPEC.md section 9.
   - Section 10 (Role Configuration) filled in 2026-09-01 -- see open item 2. GITHUB-PLATFORM-ADAPTER-SPEC.md section 6's automation trigger is now fully specified against a real role (`ROLE-001` Business Owner = Manali); the only remaining gap is a real GitHub username, which needs a real repository to exist first (open item 8).
2. **FRAMEWORK-CONFIGURATION-SPEC.md is incomplete -- sections 10 and 11 filled 2026-09-01.** Sections 9 (Artifact Configuration), 10 (Role Configuration), 11 (Approval Gate Configuration), 13 (Platform and Storage Adapters), and 15 (Traceability Configuration) are filled in. Section 10: solo-operator instance on a personal GitHub account -- 9 roles as individual rows. `ROLE-001`-`ROLE-005` (Business Owner/Architect/Engineering Reviewer/Design Reviewer/Producing Designer) all held by Manali; `ROLE-006`-`ROLE-008` (Business/Technical/Developer Agent) as agent identities, carried forward from AGENT-RESPONSIBILITIES.md and DESIGN-HANDOFF-BUNDLE-TEMPLATE.md rather than invented; `ROLE-009` (Compliance/Gate Assistant Agent) added the same day after Manali asked whether any of her reviews could move to a different agent -- it runs the mechanical completeness checklist at every gate (never the judgment call), and holds final authority only for Source Readiness (data completeness, not business/technical/design judgment). Section 11: six gates (GATE-001 Source Readiness through GATE-006 Implementation PR Review), each `ROLE-009` pre-check + the accountable role's final call, mapped to the outcome tables each spec already defines (PRODUCT-SOURCE-MATERIAL-SPEC.md §9, DESIGN-HANDOFF-BUNDLE-TEMPLATE.md Part 4, DESIGN-ANALYSIS-REVIEW-SPEC.md §11-12, BUSINESS-PR-SPEC.md §9-10, CANONICAL-TASK-SPEC.md §9). Two things explicitly left open in section 19: no fallback authority exists (one identity, nothing to fall back to), and Design Reviewer/Producing Designer being the same identity still means the final Bundle Acceptance call is self-review even though `ROLE-009`'s pre-check is now independent. **All 16 configuration sections are now filled in -- completed 2026-09-01.** Sections 6 (Source Configuration), 7 (Analysis Modules), and 8 (Lifecycle Configuration -- framed explicitly as this repository's kanban board columns, per Manali's stated intent) were filled by synthesizing already-defined rules from PRODUCT-SOURCE-MATERIAL-SPEC.md, DESIGN-ANALYSIS-SPEC.md, and ARTIFACT-RELATIONSHIP-MODEL.md; `STAGE-008` (Outcome Validation) is honestly recorded as disabled with no compensating control, rather than inventing one -- that gap is open item 11. Section 12 (Review Package Configuration) maps each of section 11's six gates to its actual review-package contents. Section 14 (Delivery Configuration) defines the full registration process but leaves `TARGET-001` an unfilled instance -- same blocker as everywhere else needing a real repository (item 8); this is what PRD.md section 13's "how are engineering repositories registered, selected, and authorized" question was waiting on, now resolved as "a row in this table, added when a real repository exists." Section 16 (Change and Version Configuration) consolidates REQUIREMENTS-VERSIONING-SPEC.md, PRODUCT-SOURCE-MATERIAL-SPEC.md section 12, CANONICAL-TASK-SPEC.md section 12, and ARTIFACT-RELATIONSHIP-MODEL.md section 6 into four change-type rows -- no new policy, just naming which existing rule governs each.
3. ~~No automation design yet for detecting a merged, approved Business PR and invoking the Technical Agent~~ -- corrected 2026-09-01, this was stale: specs/GITHUB-PLATFORM-ADAPTER-SPEC.md section 6 IS this automation design (a GitHub Action checking Business Owner review + Stage Trace completeness), and section 10's Role Configuration now makes the "who is the Business Owner" check concrete. It's designed, not built -- item 8 (no real repository) is the only remaining blocker. A sibling automation for the *front* of the pipeline was added the same day -- see item 13 below.
4. ~~No TECHNICAL-AGENT-WORKFLOW.md mirroring BUSINESS-AGENT-WORKFLOW.md for the Technical Agent side~~ -- **written 2026-09-01**, see specs/TECHNICAL-AGENT-WORKFLOW.md. Mirrors BUSINESS-AGENT-WORKFLOW.md's resolved shape (input contract, 9 ordered processing steps, output contract, MUST NOT list, diagram, failure/blocking conditions, compliance checklist) and closes all three open questions it was expected to in one pass: Technical Plan format (section 4.1 -- optional, default none, Markdown when used, additive `Technical Plan` field added to CANONICAL-TASK-SPEC.md section 6), the engineering-PR reference format (section 6.1 -- `Traces to: TASK-<id> (business repository: <name>)`, concretizing GITHUB-PLATFORM-ADAPTER-SPEC.md section 3's existing rule), and change management after Tasks already exist (section 8.1 -- turned out to already be fully defined in CANONICAL-TASK-SPEC.md section 12; PRD.md/IMPLEMENTATION-PLAN.md's tracking just wasn't cross-referencing it, now fixed). No open decisions left in its own section 10.
5. ~~No Source Material registration template~~ -- fixed: see PRODUCT-SOURCE-MATERIAL-TEMPLATE.md and SOURCE-REGISTRY.md.
6. **Two diagrams from earlier in this work were built as HTML/SVG** (a wiring diagram and a fan-out detail diagram, published as Cowork Artifacts, not part of this repo's files) before the no-HTML preference was established. They have not been redone in PlantUML/Mermaid -- only do so if asked.
7. **Dry run through the Business PR gate -- awaiting the actual Business Owner decision.** "Cart optimization" (remove item from cart / apply coupon) is running as a real instance, no git yet, plain files only:
   - Design Handoff Bundle: DONE -- `design/checkout/cart-optimization/v1.0/` (bundle.md + 2 screens + 1 flow).
   - Source registration: DONE -- `sources/checkout/cart-optimization/source-cart-optimization-SRC-001.md`, indexed in SOURCE-REGISTRY.md.
   - Design Analysis: DONE -- `analysis/checkout/cart-optimization/design-analysis-cart-optimization-DA-001.md` (DA-001). Produced 4 Business Requirements (BR-001..BR-004), 2 gaps (GAP-001 no mobile frames, GAP-002 unclear "editing" quantity state -- correctly NOT turned into a requirement), 4 open Decisions, 2 Technical Unknowns.
   - Business Requirements: DONE -- `requirements/checkout/cart-optimization/business-requirements-cart-optimization-REQSET-001.md` (REQSET-001), carrying BR-001..BR-004 forward unchanged with full evidence/classification.
   - Epic/Stories + Business PR: DONE -- `business-prs/checkout/cart-optimization/business-pr-cart-optimization-BPR-001.md`. EPIC-001 "Cart Item Management" with 3 Stories (STORY-001 remove item, STORY-002 apply coupon, STORY-003 empty-cart return), each with a complete Stage Trace. Status: In Review. **This PR deliberately combines DA-001 and REQSET-001 review into one Business Owner decision -- an explicit, stated resolution of BUSINESS-PR-SPEC.md section 13's open question, not a silent default.**
   - Storage convention used for this dry run (not yet formally added to any spec's own storage section): `requirements/<feature-slug>/business-requirements.md` and `business-prs/<feature-slug>/BPR-<number>.md`, mirroring the `design/` and `analysis/` pattern. Feeds open item #1.
   - **APPROVED 2026-08-31.** Manali approved BPR-001 as submitted (section 12). DA-001 and REQSET-001 statuses updated to Approved to match (this PR bundled both reviews). The four open Decisions (DEC-001..DEC-004) were explicitly NOT resolved by this approval -- they remain open and carry forward, recorded as such in BPR-001 section 12's notes. The precedent of combining Design Analysis + Business Requirements review into one PR was used for this PR but not separately confirmed as standing policy -- still worth asking about before assuming it's the default for the next one.
   - Per BUSINESS-PR-SPEC.md section 11, merge of an approved Business PR is the handoff trigger to the Technical Agent. There is no real git/GitHub in this repo yet (open item 1), so there is no literal "merge" -- approval is recorded directly in the files instead. If/when git is set up, this is the concrete case to test that trigger against.
   - Technical Agent stage stays illustrative-only from here -- no real engineering repo exists to run it against for real.
   - This still leaves the general point open: the Technical Agent side and automation remain untested against this or any other real example. The business side of the dry run is now complete end to end.

8. ~~No real git/GitHub repository exists yet.~~ **Resolved 2026-09-03**: real repo live at https://github.com/manalimatkar/product-management, personal account, first commit pushed. `GITHUB-PLATFORM-ADAPTER-SPEC.md` section 6's Business Owner check is filled in with the real username -- `ROLE-001`'s row in FRAMEWORK-CONFIGURATION-SPEC.md section 10 now carries `manalimatkar`, done the same day. The GitHub Actions themselves (sections 6 and 6.1's automations, `TARGET-001` in section 14) are still not wired up -- the repo exists, the workflow YAML files don't yet. Note on working with this repo going forward: this connected folder blocks file deletion by default, which causes git to leave a harmless stray `.git/index.lock` file behind after most commands (git itself can't clean up its own lock) -- a `git status` warning about it is expected and not a sign anything is broken; clearing it (rename, not delete) before the next git command is the workaround in use.
9. ~~Root had 29 doc files with no organization~~ -- fixed 2026-09-01: see Document inventory above. `specs/` (18 files, the rules) and `templates/` (6 files, fillable formats); root kept to README.md, CLAUDE.md, PRD.md, IMPLEMENTATION-PLAN.md, SOURCE-REGISTRY.md. Filenames unchanged, only folders. All 202 internal markdown links repo-wide rewritten and verified to resolve.

10. **Ownership and due dates for unresolved business Decisions are not defined anywhere.** Surfaced 2026-09-01 auditing PRD.md section 13. Real, not theoretical: BPR-001's own four open Decisions (DEC-001..DEC-004) have no owner or due date tracked in this repository today -- they just carry forward PR to PR. Reviewed again 2026-09-01 while finishing the remaining framework documents (items 2 and 4) -- deliberately left open rather than folded in: resolving it means adding a due-date field and an escalation rule to DESIGN-ANALYSIS-SPEC.md's Decisions and Open Questions structure (section 4.10), which is a real design decision of its own, not a synthesis of what's already stated elsewhere the way items 2 and 4 were.
11. **No post-delivery validation against the approved design and business outcome.** Surfaced 2026-09-01 auditing PRD.md section 13 and IMPLEMENTATION-PLAN.md. Nothing in this framework currently closes the loop after implementation. Lowest urgency of the open items -- nothing upstream has been exercised against a real repository yet (item 8), so there's nothing to validate against in practice. This is also FRAMEWORK-CONFIGURATION-SPEC.md section 8's `STAGE-008` (Outcome Validation), now explicitly recorded there as disabled with no compensating control -- same gap, now visible in two places instead of one, not yet closed in either.
12. **Storage hierarchy gained an optional app layer -- decided 2026-09-01.** `product-slug` is now `platform-slug` throughout every spec and template. A new, optional `app-slug` level sits between platform and feature, used only when a platform genuinely contains multiple distinct applications sharing one stack (the trigger case: managing multiple applications of the same monolith -- illustrated, not built, with a hypothetical example in ARTIFACT-STORAGE-SPEC.md section 6). Platform is always present; app is omitted for a flat platform. `checkout` (the dry run's platform) has no app-level subdivision, so no files moved -- only terminology changed. CANONICAL-TASK-SPEC.md gained a "Target Platform" field alongside the existing "Target Application" field, the latter now explicitly conditional on the platform having an app layer. Shared code/components across apps are explicitly out of scope for this repository -- they live in an engineering-side shared library, not in this business-artifact pipeline; a genuinely platform-wide *business* initiative, if one ever arises, gets its own app-slug named at that time rather than a pre-built `shared/` folder.

13. **Design Handoff Bundle upload is now a PR whose merge does two jobs at once -- decided 2026-09-01.** GITHUB-PLATFORM-ADAPTER-SPEC.md section 6.1 (new): a GitHub Action on the bundle's upload PR combines `GATE-002` (Bundle Acceptance, `ROLE-004` sign-off, sets `status: Approved`) and `GATE-001` (Source Readiness, mechanical, sets `readiness` from whether the bundle's `knownLimitations` field is empty) into one merge event, since a Design Handoff Bundle is registered as source material the instant it's Approved -- there was no reason to hold the two gates apart in time. That merge comment is what satisfies BUSINESS-AGENT-WORKFLOW.md section 3's entry condition; the Business Agent's own step-1 source validation stays a defensive drift-check, not a second readiness judgment (this was discussed directly -- Manali questioned the overlap, and the distinction held up). Also resolves DESIGN-HANDOFF-BUNDLE-SPEC.md section 11's open question: `readiness` is written back into `bundle.md` itself, not tracked only in the source registry. Same real-repository blocker as item 3.

14. **Automation-phase roadmap started 2026-09-03; item 1 (real repo) done, item 2 (Execution Adapter spec) done.** Manali's stated goal for this phase: a platform-agnostic automated system (tools/skills/agents runnable on Claude, ChatGPT, or Copilot interchangeably), with the ability to run any single pipeline stage independently rather than only a full end-to-end automation. Proposed 5-item roadmap: (1) real git/GitHub repo -- DONE, see item 8; (2) Execution Adapter spec (which platform executes a stage, distinct from GITHUB-PLATFORM-ADAPTER-SPEC.md's storage adapter) -- DONE, see specs/EXECUTION-ADAPTER-SPEC.md, scoped to the invocation contract only, specifying Claude/ChatGPT/Copilot together rather than Claude alone; (3) a platform-neutral Tool Contract per pipeline stage (concrete callable functions derived from BUSINESS-AGENT-WORKFLOW.md/TECHNICAL-AGENT-WORKFLOW.md's Processing/Output tables) -- not started; (4) one reference implementation of one stage on one platform, recommended Business Agent on Claude -- not started; (5) a stage-level platform-agnostic runner/orchestrator -- not started. Do not start 3-5 without Manali explicitly redirecting to them.
